# Decision Log — trading-bot

Append-only. Newest last. Full rationale lives in handoff.md §5 and docs/.

| # | Date | Decision | Rationale |
|---|------|----------|-----------|
| 1 | 2026-08-22 | Ground-up rebuild to revised spec (rule engine, GateDecision, regime, narration-only LLM); old backend/frontend removed (in git history) | New architecture supersedes old filter/LLM-scorer design |
| 2 | 2026-08-22 | All Python in backend/, run from inside it; frontend separate | User-requested separation; sibling imports unchanged |
| 3 | 2026-08-22 | market_regime table w/ candidate_count; provider_call_counters w/ dedicated 429 count | Regime history queryable; capacity signal distinct from errors |
| 4 | 2026-08-22 | Grounding validation = rule-derived vocab + invented-rule check + numeric echo; flags recorded not dropped | Spec D2; avoids stale central keyword lists |
| 5 | 2026-08-22 | Atomicity: conditional write → rowcount → cash; unique open-per-mint index; scale cap inside UPDATE WHERE | §5.1 highest-stakes property; proven by tests |
| 6 | 2026-08-22 | Birdeye discovery via token_trending?listing=memepool | tokenlist returns majors, not memecoins (verified live) |
| 7 | 2026-08-22 | Jupiter migrated to lite-api.jup.ag/swap/v1/quote | v6 quote-api sunset (DNS dead), verified live |
| 8 | 2026-08-22 | Decimals threaded Candidate→snapshot→price calls; fail-closed when unknown | CRITICAL: 9-decimal assumption fabricated 1000× prices (BARRON +96k% bug) |
| 9 | 2026-08-22 | token_security 401 → non-retryable + session auto-disable + semaphore(2) | Free tier lacks endpoint; retry storms stalled ticks |
| 10 | 2026-08-22 | qwen3 think=false + /no_think + strip think blocks | Thinking mode made narrations take minutes at ~23 tok/s |
| 11 | 2026-08-22 | Backend serves built frontend (SPA catch-all after API routes) | Single origin/port enables true one-click app |
| 12 | 2026-08-22 | start.sh/stop.sh/.desktop launcher; ollama started only if absent; stop never kills pre-existing ollama | User-requested single-click startup |
| 13 | 2026-08-22 | the reference bot comparison doc; commit-reveal verified byte-for-byte; proof stays unbuilt | Validates architecture divergence (agent layer closed) and appendix accuracy |
| 14 | 2026-08-22 | Corrupted DB wiped after decimals-bug closes ($481k fake cash) | Day-1 test data; honest reset for calibration |
| 15 | 2026-08-22 | Dual-lens discovery: Birdeye SUBSCRIBE_TOKEN_NEW_LISTING websocket (buffered, session auto-disable) merged with trending; Candidate.discovery_source observability-only; rules never read it | Docs confirm no REST new-listing param exists; WS is Birdeye's sanctioned mechanism; decision coupling explicitly forbidden (Task A) |
| 16 | 2026-08-22 | Thesis reuse: reuse prior narration iff verdict+failed-set identical AND liq/vol ≤5%+$250, mcap ≤5%, txns ≤10%+25, chg ≤3pp, within 3 ticks; source marked "reused" | Cuts duplicate Ollama load + feed noise on unchanged rejections; rule engine still re-evaluates every tick |
| 17 | 2026-08-22 | OLLAMA_NUM_CTX=1024 explicit in generate options; provider.aclose() added to main() shutdown | KV-cache RAM scales with num_ctx not prompt length; prompt analytically <700 tok; verify via prompt_eval_count. Shutdown gap: provider never closed |
| 18 | 2026-08-22 | Task C: live_execution/ package at repo ROOT (outside backend/); own EXECUTION_ENABLED flag default-off; imports decimals math FROM backend.data_providers.jupiter (no drift); unknown decimals refuse before any network call; operator-invoked CLI only | Isolation contract: backend must never import live_execution (test-enforced). Real-money code stays decoupled from paper pipeline per defense-first skill |
| 19 | 2026-08-23 | Task C rebuilt to full seven-file safety model: hardcoded LIVE_TRADING_ENABLED=False + REQUIRE_MANUAL_CONFIRMATION=True (env bypass for arming REMOVED), confirmation queue w/ fail-closed expiry re-checked at consume, persistent kill switch + auto daily-loss breaker (-$75 realized), idempotency ledger, exposure/position caps, wallet module, confirm_trade.py CLI; swap URL verified by real POST (422 names quoteResponse field) before hardcoding | Scaffold never arrived + prior partial impl lacked the described safety model; env-settable arm flags are one stray .env line from disaster; approvals must not outlive their window even once granted; user approved rebuild option |
| 20 | 2026-08-23 | reference-mimicry: exit engine rebuilt on the reference bot model — 6 rules (stop −20%, HWM trail 50%/40pp, liquidity break <$8k, invalidation −25%&1.4×sells, stale 14d, TP ladder +100/300/900 trims) + sell risk gate; dedicated 15s price-only exit scan loop; DB migration high_water_usd/tranches_taken | DB forensics: stops realized −40.2% avg vs −20% config when checked once/tick; DONT churn −$709 via unchecked re-entries; risk-off bypasses sell gate deliberately (blocking a stop during a cascade bleeds the book) |
| 21 | 2026-08-23 | Old decision logic purged; entry gate = the reference bot rules verbatim (liq $15k, vol $8k, newborn 24h/−15%, strict already_held, not_on_break); scale-ins deleted entirely; crowd_heat act-band [36,100] w/ presence proxy until FOMO feed wired; liveness module added | User approved full mimicry; strict already_held makes pyramiding structurally impossible (DONT pattern killer); one-position-per-name is the reference's own gate behavior |
| 22 | 2026-08-23 | Crowd conviction = REAL fomo.fun board via Privy session with AUTO-RENEWING auth (rotated refresh tokens persisted to gitignored state file; state beats stale .env bootstrap); Firecrawl stealth fallback with MULTI-PROVIDER chain (scrapingbee/scrapingdog/zenrows/scrapeops, quota-exhaustion benching); enrichment gated to DATA_BACKEND=live for mock hermeticity; pump.fun comments evaluated and DEFERRED (API dead/503s via stealth) | Live validation proved direct reads Cloudflare-challenged even with valid bearer (the reference documents the same and uses the identical firecrawl-stealth fallback); self-sustaining auth chain ends manual token re-extraction; hermeticity leak flipped all e2e verdicts once — never let live feeds touch mock runs |
| 23 | 2026-08-23 | THINK stage implemented (llm/thinker.py): qwen3 writes {thesis, invalidation, verdict} BEFORE rules; entry requires model buy AND all-rules-pass (think→gate intersection); Ollama-down degrades to deterministic template tagged degraded:*; book reset to fresh $1,000 | Live tick proof: CYBERLEEK passed all 11 rules but MODEL VETOED it (old logic would have entered); DONT refused by both layers; fail-closed when Ollama down; thinker ~10s/candidate — watch tick cadence vs MAX_CANDIDATES_PER_TICK |
| 24 | 2026-08-23 | Security audit (scans CLEAN; stale files removed) + OPTIONAL Supabase Postgres backend: db_pg.py (asyncpg) mirrors db.py's full surface; db.py delegates when USE_SUPABASE_DB=1+SUPABASE_DB_URL; pytest guard FORCES SQLite in tests; migration migrations/supabase/001_init.sql (9 tables, RLS locked); TLS via SHA-256 cert-fingerprint pinning (pooler cert self-signed; TOFU pin .supabase_fp.txt gitignored; mismatch=hard abort). Stealth chain upgraded: scrapeops keep_headers + zenrows custom_headers(+premium_proxy) FORWARD the Privy bearer — verified live pulling REAL board data through prod-api Cloudflare; scrapingbee cannot (platform eats Authorization). BUGFIX _json_from_body: rejected statusCode-keyed bodies incl. prod-api's statusCode:200 SUCCESS envelopes — now only ≥400 | Live smoke vs real Supabase passed every §5.1 atomicity check (dup-open refused, idempotent close, cash guard refuse/apply/restore) then self-cleaned; uvicorn boot served PG data on /api/stats,/api/system-status,/api/holdings,/api/feed; 145 tests pass on forced SQLite; header forwarding proven via httpbin echo + real BONK thesis reads |
| 25 | 2026-08-24 | PG cutover bugfix: FOUR files had raw SQLite SQL outside the db layer (journal.py is_open=0; proof.py is_open=1 ×2; main.py thesis UPDATE w/ ?-placeholders; paper_trading_engine.py DELETE rollback) → Supabase 42883 boolean=integer every ~15s (dashboard polls). Moved ALL into db layer: 7 new repo fns in BOTH backends (count_closed_trades, get_recent_decision_commits, get_recent_fills, get_open_position_marks, get_verify_commits, set_trade_thesis, delete_trade_row). RULE: zero raw SQL outside api/db*.py. Also: start.sh setsid + ollama-list timeout (Konsole Ctrl+C/tab-close and a hung ollama list were killing/blocking launches); _ApiKeyRedactor log filter in crowd.py (setup_logging never runs under uvicorn); stale default_ledger import removed from proof.py | Supabase 42883s stopped; all 6 endpoints 200; verify.json 80/80 seals match; 0 tracebacks in current instance; 145 tests pass; backend.log scrubbed (17 plaintext keys removed) |
| 26 | 2026-08-25 | Dashboard overhaul (user-requested): feed ENTER/PASS labels; `[model veto]` prefix removed — thesis verbatim (fixed double "invalidates if"); expanded feed shows full model answer + mint contract address w/ click-to-copy + amber "model chose not to enter:" block when rules pass but model declines; /api/stats +realized_pnl_usd/+unrealized_pnl_usd (live marks, None-safe)/+total_spend_usd (open cost basis incl fee+slippage); UI stats = exactly those five numbers, equity-curve chart removed from UI (API field kept); knowledge tab + PaperTradingBanner deleted (KB unused by prompts — verified; backend endpoint untouched) | Operator UX: rejection reason must be visible without decoding tags; veto tag redundant once thesis is shown as-is; unrealized math reuses compute_unrealized_pnl via provider (fail-soft on missing prices); PAPER_TRADING_ONLY untouched; 145 tests pass, vite build clean |
| 27 | 2026-08-26 | Planned LLM API migration: DeepSeek V4 Flash direct API for thinker; Groq retained for evidence-only social reads; reflections/reports queued off-peak; add token/cost/latency/cache/model/prompt/outcome accounting before switching | DeepSeek is the first cost/quality candidate, but superiority must be proven in shadow replay and paper canary; provider failure must fail closed to thinker pass; no automatic learning, threshold, prompt, model, or live-trading changes |
| 28 | 2026-08-26 | Verified live_execution wiring end to end and repaired Solana drill endpoint selection plus place_buy preflight ordering; keep hardcoded LIVE_TRADING_ENABLED=False and mandatory manual confirmation | The root bridge, Jupiter quote/swap, local signing, rotating RPC confirmation, commit binding, ledger, and manage/sell path are connected; offline tests pass, but funded throwaway-keypair devnet validation remains mandatory before any mainnet consideration |
| 29 | 2026-08-26 | Implemented REF-R5 durable events and weighted memories in both DB backends; recalled lessons are injected into thinker context only, with read-only recent-events API | Preserves deterministic gate authority while making accumulated observations auditable and available to future thinker calls; memory recall increments hits and all event kinds are validated |
| 30 | 2026-08-26 | Implemented LLM API migration: extracted `LLMClient` base + `MainGroqClient` (thinker/narrator, `qwen/qwen3.8-27b`) + `GroqClient` (social reads); wired full token/cost/latency instrumentation into `llm_call_usage` table; added `shadow_replay.py` and `outcome_labels.py` scripts | DeepSeek was the original target but required a funded key; Groq with qwen3.8-27b is the interim primary provider. Migration is fail-closed (template `pass` on provider failure); no automatic promotion permitted |
| 32 | 2026-08-27 | Supabase schema-drift incident fixed: live book was missing `events`/`memories`/`theses` (added in-place to 001_init.sql after it was first run) and `llm_call_usage` + versioning columns (002_llm_usage.sql never applied) → every tick died at `insert_event`, `/api/system-status` 500'd on every poll. Fix: `db_pg.init_db()` now runs idempotent `_SCHEMA_SYNC_SQL` (CREATE TABLE IF NOT EXISTS + ADD COLUMN IF NOT EXISTS + RLS + `schema_migrations` bookkeeping) and refuses boot if healing fails; 002 migration file gained the same RLS + bookkeeping. Also fixed three db_pg surface-identity bugs: `status_str`→`status` param (latent TypeError), `get_llm_call_usage` `::text` casts (latent AttributeError in learning_loop), feed-event `model_version`/`prompt_version` parity | Self-healing chosen over manual SQL re-run because in-place drift already happened once; verified live: schema synced on boot, 0 errors, first full tick completed, all endpoints 200, 231 tests pass. RULE: never edit an applied migration in place — new schema = new numbered file + mirror into `_SCHEMA_SYNC_SQL` |
| 31 | 2026-08-27 | Bug audit of Groq migration: fixed 7 bugs — stale `config.DEEPSEEK_MODEL` references in thinker/narrator (runtime crash), wrong `narration_mode` label in system_status endpoint, `usage.mint_address` AttributeError in social.py (LLMResult is a dataclass), `from backend.llm.client` import path in loader.py (wrong when running under uvicorn from backend/), stale pricing branch in client.py (dead deepseek branch with wrong rates), and broken ollama references in start.sh. All Python files syntax-check clean; LLM ping test passes for both Main and Social Groq | Groq API live and healthy; shadow replay and outcome labels in scripts/; DeepSeek migration deferred pending funded key |
| 33 | 2026-08-27 | Executed §18: main LLM path (thinker/narrator/reflections) moved to DeepSeek V4 Flash direct API (non-thinking mode); `MAIN_LLM_PROVIDER` env selector (default groq, live .env flipped to deepseek); `DeepSeekClient` + `build_main_client()` factory (unknown value fails closed to groq); DeepSeek cost branch with peak/off-peak + cache-hit rates verified against api-docs.deepseek.com; provider-aware timeouts; reflections skip to template in DeepSeek peak windows; social reads untouched | Funded key arrived. Shadow replay gate passed first (8/8 verdict agreement, 100% valid JSON, p95 2.6s) then flipped. Two latent bugs fixed en route: `is_peak_window` never computed (costs understated 2× at peak) and V4 Flash default THINKING mode emptied `content` (fixed with `thinking:{type:disabled}` — non-thinking per docs/08 §1). Fail-closed proven live: pre-fix window degraded to template passes with zero entries. 30 new tests, 261 total. Rollback = one .env line (GROQ_API_KEY currently empty → rollback degrades fail-closed until re-added) |
| 35 | 2026-08-27 | Ported the reference repository's LLM *reasoning layer* (not its execution) into `backend/llm/llm_brain.py`: `run_role()` role-based router with honest resolution + ordered fallback + unsupported-model bench; brain tick prompt (hard filters, 6 decision buckets, ground-truth + price-talk rules, minified-JSON contract, persona lore dropped); wallet mimicry + None-safe snapshot builder; strict `parse_llm_tick()` validation; `LLMBrain.tick()` grades ≤8 highest-volume candidates fail-closed. `main.py` uses the brain's verdict per candidate in live mode (falls back to per-candidate thinker otherwise); the reference `buying`→our `buy` is NECESSARY only — deterministic gate still ANDs. Config `LLM_BRAIN`/`LLM_BRAIN_MAX_TOKENS=4000`/`LLM_BRAIN_TIMEOUT_SECONDS=60` | Operator: "compare the reference's thinker and clone it to ours." Cloned reasoning only so paper→live is just the execution switch. Fixed a pre-existing tick-crash: `reused_if_stable()` required `prior["stats"]` but the thesis writer never stored it → `KeyError` killed the tick (writer now stores stats + reuse fails closed on malformed prior). Live-verified: brain ping graded 8/10 (`DELTA=buying`, 6 checks + invalidation, 2268 tokens, no truncation); fail-closed proven on a truncated response; backend clean. LLM stays veto/input only; `live_execution` untouched; `PAPER_TRADING_ONLY` asserted. 27 new tests, 289 total |

| 36 | 2026-08-27 | De-branded the repo and renamed the brain module to `backend/llm/llm_brain.py` (brain class → `LLMBrain`, verdict type → `LLMVerdict`, parser → `parse_llm_tick`, system prompt → `LLM_SYSTEM`, config → `LLM_BRAIN*`); test files → `test_llm_brain`/`test_ref_r*`; removed the scratch `backend/scripts/verify_reference_commit.py` (its only purpose was byte-for-byte verifying the reference's external on-chain preimages — not imported anywhere, recoverable from git). Repo-wide replaced the upstream project's branding with neutral "the reference"/"reference" prose and `REF-R#` requirement ids | Operator: rename the brain module to llmbrain.py and remove any references to the upstream project's name from the whole repo. Kept `fomo`/`FOMO` (the crowd-board product), `promotion`/`promotion_gate`, and token tickers (BLOSSOM/SEAL) — all unrelated to the brand. The reference's `commit-v1` memo prefix was NOT our commit scheme (main.py + commit_log.py both hash `sha256(nonce|canonical)` with no prefix) — it only lived in the removed scratch verifier + two reference-comparison docs, so renaming it breaks nothing. 54 files edited (pure find/replace, 407/407 balanced), 5 renames, 1 deletion. All 289 tests pass; backend restarted clean on the renamed module (0 import errors, 0 tracebacks) |
| 34 | 2026-08-27 | Stealth-scrape chain: split HTTP 402 (credit exhaustion → long `STEALTH_BENCH_SECONDS` bench) from HTTP 429 (transient rate-limit → short `STEALTH_THROTTLE_BACKOFF_SECONDS`=75s backoff) via `_handle_provider_status()`; provider's own error body now logged on both so quota reasons are self-diagnosable; empty scrape exceptions log their type | Live-diagnosed operator report ("firecrawl/zenrows have credits, only bee is full"): Firecrawl WAS healthy but a 429 rate-limit benched it 30 min (the bug — now short backoff); ZenRows key in `.env` is genuinely at usage limit (AUTH004 on both cheap AND premium requests — operator may be viewing a different account/key); ScrapingBee basic tier answers 200 but stealth-proxy errors and it can't forward the Privy bearer anyway, so it never serves fomo reads. +1 regression test, 262 total |
| 37 | 2026-08-27 | REF-R8 drawdown-adaptive risk budget: verbatim port of the reference `computeBudget()` into `paper_trading_engine.py` (`RiskBudget` + `compute_risk_budget` + `portfolio_equity_and_unrealized`), new opt-in `SIZING_MODE="risk_budget"` (default stays `"fixed"`), hardcoded constants `PER_ORDER_FRACTION=0.035`/`DAY_MULTIPLE=4`/`HARD_ORDER_CEILING_USD=3000`/`HARD_DAILY_CEILING_USD=12000` (never env-overridable), derived daily ceiling enforced at entry with journaled refusal, budget published on `/api/disclosure.json` | Handoff §22 approved pair (paper-compatible, no arming). Sizing shrinks when the book is under water on open risk — a desk rule expressed as checkable arithmetic; model still decides WHETHER, never the size. Parity verified against reference source incl. the `MIN_TICKET_USD` clamp low bound ($1000 at −20% → $25) and Math.round half-up rounding. Fail-closed: unreadable book → minimum ticket, never raises into the tick. 30 new hand-computed tests |
| 38 | 2026-08-27 | REF-R9 closed-loop conviction factor: new pure `backend/calibration.py` (verbatim `computeCalibration()` port: expectancy → raw scale +10%→+20%/−10%→−40%, confidence `min(n/12,1)`, factor clamp [0.6,1.2], FLAT 1.0 on no/invalid data); persisted via new `patch_daily_stats()` key-merge into `daily_stats.stats_json` in BOTH db.py and db_pg.py (JSONB `\|\|` merge, no schema migration, mirrors reference `omo_meta`); multiplies the R8 budget in risk_budget mode; learning_loop persists it too; disclosure surfaces factor + formula | Closes the loop: what happened after a trade now changes what the next trade may be — arithmetic only, NOT model output, NOT automatic threshold changes (those stay manual per §15). Small samples pulled to neutral so three trades cannot rewrite the book. 12 new hand-computed tests; combined suite 331 passing; live smoke: tick persists real budget + FLAT calibration, disclosure serves both, 0 tracebacks |
| 39 | 2026-08-27 | Dead-provider fail-fast: a stealth scraper that fails transport (timeout/connect) twice IN A ROW is benched exactly like a 402 (`_CONSECUTIVE_ERRORS` streak, reset on any response); `_scrape_firecrawl` wrapped in try/except (was uncaught); stealth timeout 45s→25s and direct read 2 transport attempts (reference parity) | Operator-reported ~15-min tick stalls. Root cause: ScrapingBee ReadTimeouts were caught+logged but never benched, so every candidate re-tried it (~20×45s). Reference audit (verbatim source) showed its fomo fallback is the same Firecrawl stealth-proxy we already call — no free scrape mechanism exists; the gap was timeout discipline, not capability. A dead provider must cost a couple of timeouts ONCE, never one full timeout per candidate per tick. 6 new tests (23 in test_crowd.py); 337 combined passing; live-verified: ScrapingBee benched after exactly 2 timeouts, crowd stage degrades in seconds, 0 tracebacks. Refilling Firecrawl credits restores real crowd heat with zero code changes (chain self-heals) |
| 40 | 2026-08-27 | Fresh scraper keys activated (Firecrawl/ScrapingBee/ScrapingDog/ScrapeOps in root `.env`; ZenRows unchanged/exhausted) + backend restart to load them and clear benches; `_scrape_scrapingdog` wired to forward the Privy bearer via `custom_headers=true` + `fwd_headers` (ScrapingDog docs: same mechanism as ScrapeOps `keep_headers`, no extra cost) | Operator added new keys. Restart was required because config reads env at import and bench state is in-memory/per-process. ScrapingDog was already in the chain but useless for fomo (no header forwarding, and prod-api needs the bearer) — its docs confirm `custom_headers=true` forwards request headers to the origin, so the fix mirrors the proven ScrapeOps pattern. Live-verified after restart: Firecrawl 15× 200 OK (real crowd heat restored), ScrapeOps 1× 200 OK failover (caught a transient firecrawl 500), 0 tracebacks. ScrapingDog returns 403 (key plan excludes Web-Scraping API, or base proxy can't pass Cloudflare — may need premium proxy); it's a harmless backup since Firecrawl+ScrapeOps cover it. +1 test (338 combined) |
| 41 | 2026-08-27 | REF-R11 on-chain precommit memo (commit–reveal) + micro-bootstrap: every armed order seals `sha256(nonce\|canonical_payload)`, publishes the hash on-chain as a Solana memo (`commit:v1:` prefix, SPL Memo program `MemoSq4g…`) BEFORE the fill, fail-closed (an unconfirmed memo BLOCKS the fill). New `live_execution/memo.py` (solders build + `publish_commit_memo`); `commit_log.py` `sealed→published→bound` + `record_memo()`/`fail()`; `executor.py` memo-before-quote ordering + `OrderResult` carries seal+memo; `solana.get_usdc_balance()`; `run_live_cycle.py` real-USDC cash + journals seal+memo into `decision_commits`. Verifier: `memo_signature`/`memo_slot` columns (SQLite + PG self-heal + `003_commit_memos.sql`), `bind_commit_memo()`/`get_commit_id_by_hash()`, `/api/verify.json` memo checks, `/api/disclosure.json` `commit_memo` block. Micro-bootstrap: `MIN_SOL_RESERVE` env (default 0.01 SOL), `MIN_LIVE_TICKET_USD=0.5`, `compute_ticket`/`compute_risk_budget` optional `min_ticket_usd` floor (paper bit-identical). Devnet drill sends a real memo | Operator-approved (the "implement against omotrades/omo" instruction is the §13 sign-off for this re-opened item). Reference sources: `precommit.server.ts`/`verify.server.ts`. Deliberate deviations (documented §26): fail-closed blocking (reference publishes async), immediate reveal, single signer = trading wallet, de-branded prefix. Memo precedes the quote so the quote→fill window is unchanged. Micro-bootstrap lets the book start from 0.03 SOL (fees) + $3–5 USDC (capital) and compound; the pre-commit USDC check blocks entries before any memo fee is spent. 41 new tests (379 combined); isolation grep clean; live smoke disarmed all endpoints 200 (`armed=False`). solders 0.29.0 installed; fixed a latent `Hash.from_string` bug in drill.py. Ships DISARMED — arming is the operator's FINAL task (handoff §27) |
| 42 | 2026-08-27 | omo-audit code queue A7/A6/A3/A2/A4 (handoff §29): A7 wash-trade filter (`rule_engine/fake_chart.py`, all 13 omo thresholds, READ-stage before think/gate, `Candidate.volume_5m_usd` added); A6 symbol blocklist (`BLOCKED_SYMBOLS` + `is_blocked_symbol()` in `blocklist.py`, enforced in `filter_candidates`); A3 venue attribution (`live_execution/venue.py` → `decision_commits.venue` → /api/binding.json, observability-only); A2 chain reconciliation (`solana.get_token_balances` + `live_execution/reconcile.py`: chain = authority on quantities, journal = authority on cost, ledger never mutated by a chain read, exit sizing clamped to chain truth, vanished positions excluded+flagged, unjournaled holdings never added); A4 own-basis read-back (`crowd.read_own_basis` + `FOMO_OWN_HANDLE`, cross-checked vs journal cost each live cycle, observability-only) | Operator instruction: implement the five gaps from the omo audit (docs/09_OMO_AUDIT_COMPARISON.md) against `omotrades/omo`. Reference sources fetched raw: market.server.ts (isFakeChart), blocklist.ts, wallet.server.ts (readViaRpc/getWalletSnapshot/fetchFillVenue), fomo.server.ts (readOwnBasis), execute.server.ts. Two deliberate deviations, both defense-first: (A2) omo re-derives the whole book from chain+tx history each sync, we keep the §5.1 atomic journal as money authority and use the chain as a loud cross-check + sizing clamp — a lying RPC cannot corrupt the money ledger; (A7) unknown age/fdv fields skip rather than fail-closed, since our providers don't always return them and failing would starve the candidate pool (numeric gate rules still apply after). A4 reuses the existing Privy session chain — no new secret. +65 tests since REF-R11 → **444 combined passing**; isolation grep clean; live smoke disarmed all endpoints 200 (`armed=False`, `venue:null` on unbound pairs). Ships DISARMED — §27 (enable live execution) untouched and STILL the final task |
| 43 | 2026-08-27 | A11 thesis re-authoring (handoff §30): `backend/thesis_restate.py` ports the reference `thesis-author.server.ts::restateTheses` — once per tick/cycle, rewrite open write-ups that are stale (`THESIS_RESTATE_STALE_HOURS=6.0`) or not model-authored against the position's current numbers, at most `THESIS_RESTATE_PER_PASS=2` rows/pass, oldest text first, under-60-word contract. `get_open_theses()`/`update_thesis_text()` in both DB layers (write guarded by `closed_at IS NULL`); wired into `main.py run_tick` (reuses the risk-budget block's price_map + open positions) and `run_live_cycle.py` (marks from `_manage`; outcome key `thesis_restatements`); `/api/disclosure.json` `thesis_restatement` block; usage accounted as task `thesis_restate`; each rewrite journaled as a `did` event | The 2026-08-27 re-read of `omotrades/omo` (full local clone, commit 48a86f9 — unchanged since the audit) found the module the original audit's read list missed; implemented under the standing "implement against omotrades/omo" instruction. Defense-first: NARRATIVE ONLY (can only ever change thesis text/author/updated_at — never trades/cash/sizing/exits/verdicts); fail-closed validation (reject <20/>1000 chars, old text kept, refusal logged); retired-mid-pass rows never rewritten; never raises into the tick. Performance-discipline: reuses the tick's own price marks instead of the reference's per-row tape fetch (documented deviation — zero extra network I/O); DeepSeek peak-window skip (docs/08 §5 non-urgent work). Same re-read resolved both audit caveats verbatim: `placeOrder` guards (our executor is a strict superset) and their calibration-wiring claim (still unverified — `computeBudget` takes no factor, `ticketUsd` uses crowd-heat conviction; our REF-R8×REF-R9 wiring stays ahead). 26 new tests → **470 combined passing**; isolation grep clean; live-verified: first tick advanced both stale open write-ups (aura, ANSEM) via deepseek, retired row skipped, all endpoints 200, 0 tracebacks. Code queue EMPTY again — §27 (enable live execution) untouched and STILL the final task |
| 44 | 2026-08-28 | §27 pre-flight + devnet drill (handoff §31): a session request to "move live execution into backend/ and enable it" was REFUSED per handoff §1 ("if a task ever seems to require real execution inside backend/ — stop and flag it"), §27 ("no session may arm"), and defense-first skill rule 3; the operator chose the safe path instead. Pre-flight verified all machine-checkable §27 items; first REAL keypair load found two latent fail-closed bugs, fixed in commit d8e426f: (1) wallet.load_keypair passed the file path to solders from_json (expects JSON content) — now from_bytes on the already-validated array + exactly-64-u8 check; (2) drill.py used an undefined log + run_live_cycle ran --drill before logging.basicConfig. +4 regression tests (real keypair round-trip success path — previously uncovered — wrong-length refusal, pin match/mismatch). Drill PASSED 5/5 on devnet incl. a real signed dust transfer (broadcast + confirmed, slot 489023339) and the REF-R11 publish_commit_memo path (slot 489023363) | Both bugs were invisible to the mocked suite and fail-closed (safe), but both would have blocked arming day — proof the drill earns its keep; the missing success-path test was the coverage gap that let bug 1 ship. RPC requestAirdrop is daily-limited (429 across amounts/endpoints) — faucet.solana.com web faucet is the working devnet funding route. Identity pin (EXPECTED_WALLET_ADDRESS) verified end-to-end. 474 combined passing. Arm flags untouched — LIVE_TRADING_ENABLED=False, REQUIRE_MANUAL_CONFIRMATION=True; mainnet funding + the two hand-edited flag flips + supervised --once remain the operator's human-only steps |
| 45 | 2026-08-28 | §32 cash-corruption fix + final omo audit (handoff §32): phantom-cash incident (bad quote ~2,960× → poisoned high-water ratchet → TP trim credited ≈$94k) fixed with two hardcoded fail-closed guards — `EXIT_PRICE_JUMP_MAX=50` (scan-level skip + no-ratchet, upward-only so collapses still exit) and `MAX_EXIT_PROCEEDS_MULT=200` (close/trim proceeds backstop before any state write) — plus one-off cash repair; identical jump guard in the live `_manage` loop; final full-coverage omo audit (docs/09 §F) closed all open questions: exit module exists-but-unpublished (their own test imports it), calibration factor still unwired in their sizing (grep proof), wash-trade parity confirmed | Root-cause class: exit math trusted a single unbounded price sample for a money write — both guards attack the class, not the instance, and are deliberately generous (trip only on data errors, never real moves). Live cash verified accurate by construction: chain USDC balance re-read every cycle (never accumulated), fail-closed to 0 when unreadable — the paper failure mode cannot occur live; a bad quote can at worst cause an early exit (now guarded), never phantom money. Audit verdict: no trading-critical parity gap remains; remaining deltas are UX polish (narration dedupe — queued), scale custody (memo burner key — documented §26 deviation), hosting plumbing. Operator ARMED this machine via the §27 human-only hand-edits (LIVE_TRADING_ENABLED=True, REQUIRE_MANUAL_CONFIRMATION=False); that edit stays LOCAL and is deliberately NOT committed — the repo ships disarmed and `test_safety_flags_are_hardcoded_safe_defaults` is the canary (expected red while armed). +12 tests → 486 total, 485 pass while armed |
| 46 | 2026-08-28 | §33 armed state committed + pushed (handoff §33): operator-directed ("push config as armed, no questions asked"). Committed `live_execution/config.py` ARMED (`LIVE_TRADING_ENABLED=True`, `REQUIRE_MANUAL_CONFIRMATION=False` — the operator's own §27 hand-edit; diff scanned, no secrets; keys stay in gitignored `.env`). Canary re-purposed: `test_safety_flags_are_hardcoded_safe_defaults` → `test_safety_flags_match_the_committed_state`, pinning the committed state so any silent flip either way fails loudly. Disclosure truthfulness fix: `/api/disclosure.json` `armed` previously read a nonexistent backend-config attribute (always False — it would have claimed disarmed while armed); now reads the real live_execution flag via the sanctioned function-local optional import (fail-closed False if the package is absent); test updated. Docs aligned: config.py header, README live-trading section (renamed "operator-ARMED" + explicit clone-warning), handoff §1/§3/§27/§32 + new §33, project report | The safety question was raised twice in plan mode (pushing armed means a fresh clone is armed by default) and the operator overrode it explicitly and informedly both times — the commit records the operator's decision, made after §31 drill 5/5, mainnet funding, and supervised live cycles. Load-bearing properties preserved: no env bypass exists (flags stay human-edit-only); kill switch, daily-loss breaker, caps, identity pin, SOL reserve, memo-before-fill all untouched; rollback is one line. A clone still cannot trade without a funded wallet keypair + RPC in `.env` (all gitignored). Suite fully green: 486 passing (was 485+1 expected-red). §27 is now fully COMPLETE — nothing in the handoff remains gated on arming |
| 47 | 2026-08-28 | §34 live-cycle hardening (handoff §34): two operator-reported issues with the ARMED live cycle. (1) 403-rejection benching (`backend/data_providers/crowd.py`): added `_CONSECUTIVE_REJECTIONS` + `_rejection_error`/`_rejection_success` — a stealth provider whose proxy keeps getting refused by the ORIGIN (HTTP 403) is benched after two consecutive, exactly like a 402, wired into `_scrape_get_template`. (2) Micro-bootstrap live cash rule (`run_live_cycle.py`): `LIVE_ACTIVE_RULES` = paper `ACTIVE_RULES` with `cash_available` → `_live_cash_available` (checks `cash_usd >= MIN_LIVE_TICKET_USD` $0.50 instead of `INTENDED_POSITION_SIZE_USD` $100); `run_cycle` evaluates `LIVE_ACTIVE_RULES` | (1) Firecrawl/ZenRows were out of credits (402, benched correctly) but ScrapingDog's proxy was being refused by the fomo.fun origin (403 — can't pass that endpoint's Cloudflare even with forwarded headers) and was re-tried on every candidate every tick (one wasted request + ~2-3s latency each); only ScrapeOps gets through. Benching dead-for-this-endpoint providers makes the chain converge. Kept in its own counter because `_transport_success` resets the transport streak on any completed response (a 403 IS a completed response); a 200 resets the rejection streak so a recovered provider is used again. (2) The paper cash rule sized for the $1,000 paper book refused every live entry before sizing on the few-USDC micro-bootstrap book — "not working as intended". Same "paper frozen, live threads its own floor" pattern as `compute_ticket(min_ticket_usd=...)`; paper rules + `INTENDED_POSITION_SIZE_USD` untouched (calibration-frozen). Live-verified ARMED: scrapingdog benched after 2× 403 (ScrapeOps served all 20), no `cash_available` failures, several `gate=PASS`, $5 book sizes $0.75 ≥ floor. Current refusals are the model returning verdict "pass" not "buy" (DeepSeek 200 OK, no degradation) — the model veto working as designed, not a bug. 11 new tests → 498 combined passing; isolation unchanged (live rule lives in the root bridge which already imports both sides) |
| 48 | 2026-08-28 | §35 frontend rebuild + STATE_DIR fix (handoff §35): terminal design system (`frontend/DESIGN.md` + token-only `tailwind.config.js` — mono/sleek/impeccable skills synthesized with defense-first/performance-discipline); shared primitives (`lib/format.ts` verbatim-value formatters, `components/ui.tsx` Panel/Stat/Badge/Skeleton/Empty/ErrorState); all four live panels rebuilt (LiveBook headline equity strip, LiveFeed accessible expand/collapse rows with contract copy + verbatim model answer + rule breakdown, MarketRegime, SystemStatus now showing the real reasoning model + `llm_usage_recent`); feed hydrates last 50 decisions from `/api/feed` on mount then live-appends over WS (deduped, newest-first) so reloads are never blank; old `term-*` tokens fully retired (0 refs, 0 hex literals outside the token file); fonts self-hosted. Playwright installed + `e2e/dashboard.spec.ts` (5 tests: zero console errors, panels reach data/empty never blank, expand/collapse aria-expanded, Enter-key operable, offline banner on route-abort) via `npm run test:e2e`. FOUND + FIXED while wiring: empty `LIVE_EXECUTION_STATE_DIR=` in `.env` made `os.getenv(name, default)` return `""` → `Path("")` = CWD → the live CommitLedger (`commits.json`, real order nonces) wrote to the REPO ROOT | Design intent: calm dense dark terminal, every number verbatim from the backend, `—` for unknown (never `$0.00`, never blank); no new runtime JS deps; fonts via @fontsource-variable (already installed). The STATE_DIR bug was one `git add -A` from publishing real order nonces — fixed to `or`-fallback (empty → `live_execution/state/`), stray ledger moved into the state dir, `/commits.json` + Playwright artifacts gitignored, +3 tests (`test_state_dir.py`: empty/unset fallback, explicit-override-wins). The Qwen3.8 "text-only" error the operator reported was from their own chat tool, NOT the bot (verified: Groq social 8/8 200s live, DeepSeek brain OK, 0 errored usage rows, error absent from all logs). Verification: vite build clean, Playwright 5/5, pytest **501 passing** (backend 385 + live_execution 116), restart smoke all 200, no `commits.json` at root |
| 49 | 2026-08-28 | §36 live execution UNBLOCKED + Journal/Holdings pages restored (handoff §36): operator reported "bot says enter but doesn't execute" + missing journal/holdings pages. Three stacked bugs in `live_execution/` blocked every armed order: (1) quote verb — `get_jupiter_quote` POSTed to Jupiter's GET-only `/swap/v1/quote` (405 ×3); the sell path had its own inline POST quote too — both now use a new `_get_json` helper (GET twin of `_post_json`, same retry/429 semantics); (2) `executor.py` caught `ExecutionError` in four except-clauses without importing it → NameError crashed the cycle on the first quote failure; (3) solders 0.29 has no `VersionedTransaction.deserialize` — the parse constructor is `from_bytes`; the first order past the quote (GTA6: quote 200, swap 200) died at signing. Hardening: every post-memo failure phase (quote refused/failed/crashed, impact floor, build/sign/broadcast, unconfirmed fill) now journals `logc.fail(hash, reason)` per the commit_log contract "a skipped trade must be as visible as an executed one", and the network phase catches all exceptions fail-closed — no crash can leave a commit stuck at `published` without explanation again | Live proof ARMED on mainnet: GTA6 `think=buy gate=PASS` → sealed → memo on-chain → quote GET 200 → blocked at the 2.5% price-impact floor (5.30%) → journalled `failed` with reason; zero cycle crashes since. The first fill is market-dependent (needs a candidate quoting under the impact floor); the machinery is proven end-to-end. New read-only `/api/live/executions` (CommitLog lifecycle + ExecutionLedger money movements, fail-soft like `/api/live/portfolio`); Journal + Holdings pages restored as live-only views (three-page tab bar, expandable proof rows with fail reason + commit hash + memo/fill solscan links). +5 unit tests (incl. real-solders signing round-trip + MockTransport GET-verb proof) → 506; +3 E2E → 8/8 |
| 50 | 2026-08-28 | §37 stale-holdings dust fix + FIRST REAL FILL + omo-audit items 1 & 3 (handoff §37): operator reported "bot bought then sold a token but it still shows in holdings; journal says closed" + "shows enter but doesn't enter". Root cause was ONE ledger bug with three symptoms: a reconcile-clamped FULL exit produced a sell fraction just under the 0.999 close threshold (chain/journal mismatch, e.g. 0.99889), so `ExecutionLedger.reduce_position` booked a trim and left a dust row `status="confirmed"` (OPEN) — it showed in Holdings, counted against `MAX_OPEN_POSITIONS=3` (blocking every ENTER with "would hold 4 mints"), and disagreed with the journal. Fix: threaded the exit engine's full-close intent through `models.reduce_position(..., full_close=False)` → `executor.place_sell` → `place_order` → `run_live_cycle._manage` (`full_close=(decision.action == "close_full")`); with `full_close=True` the position closes outright and PnL realizes on the FULL cost (dust written off, never left open); trim path unchanged. One-time data repair (services stopped→edit→restart) flipped the stuck `2NffKvfZ…` dust row closed (backup `executions.json.bak-dust`). Item 3: `CommitLog.reconcile_orphaned(max_age_seconds=600)` marks any still-`published` (memo on chain, no bound fill) commit older than the window as `failed`/"memo published but no fill followed (orphan reconciled)" — reuses the existing `failed` status so no UI/proof route needs a new state; wired at `run_cycle` start (cheap, idempotent, fail-soft). Item 1: narrator anti-repetition — rotating `_ANGLES` appended to the LLM prompt + rotating template openers (`_ENTER_OPENERS`/`_REJECT_OPENERS`); style-only, never changes cited rules or verdict, grounding intact | Live proof ARMED on mainnet: freeing the slot unblocked entries — PINK `think=buy gate=PASS` → memo on-chain → Jupiter quote GET 200 → swap 200 → **FILLED buy AVBN6kXd $0.66 → 491.146377 tokens** → venue attributed (jupiter router): the FIRST real fill. `think=pass -> refused` rows are the model veto working as designed. Reconciliation healed 7 historical orphans on the first cycle → commits.json `{failed:8, bound:5}`, zero ambiguous `published`. The `failed` GTA6 row is the 2.5% price-impact guard correctly refusing a 5.30%-impact trade (protection, not a bug). +10 tests (3 full-close, 4 reconcile, 3 rotation) → 516 passing; all endpoints 200, 0 tracebacks after restart |






